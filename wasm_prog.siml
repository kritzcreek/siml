let add : Int -> Int -> Int =
  \x. \y. primadd;

type Either {
  Left(Int),
  Right(Int),
}

let main : Int =
  let x = Either::Right(0) in
  match x {
    Either::Left(x) => add x 5,
    Either::Right(x) => add x 10,
  };