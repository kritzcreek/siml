let add : Int -> Int -> Int =
  \x. \y. primadd;

let const : forall a b. a -> b -> a =
  \a. \b. a;

let twice : forall a. (a -> a) -> a -> a =
  \f. \x. f (f x);

let main : Int =
  let const10 = const 10 in
  let x = twice (add 10) 8 in
  add x (let x = 5 in x);
